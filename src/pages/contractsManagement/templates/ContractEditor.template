{{#> dialog}}
<div><span>名称：</span>{{treasureContractInstanceNo}}</div>
<div class='contract-editor'>
  <div id='contract-content'></div>
</div>
<div class="modal-footer">
  <button type="button" class="btn btn-secondary" data-dismiss="modal">{{negative}}</button>
  <button type="button" class="btn btn-primary" data-dismiss="modal">{{positive}}</button>
</div>
{{/dialog}}
