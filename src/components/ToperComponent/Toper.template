<div class='c-toper'>
  <div class='c-toper-content'>
    <span>{{message}}</span>
    <div class='c-links'>
      {{#each topers}}
      <a href={{url}}>{{name}}</a>
      {{/each}}
    </div>
  </div>
</div>
